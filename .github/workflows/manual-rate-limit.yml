name: Run All Rate Limit Tiers

on:
  workflow_dispatch:  # Permite ejecutar manualmente desde la pestaña "Actions"

jobs:
  tier1:
    name: Run AGDE Tier 1
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: API-s-/Rate-Limit
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Give permission to script
        run: chmod +x run-agde-tier1.sh

      - name: Run Tier 1 script
        run: ./run-agde-tier1.sh

      - name: Upload Tier 1 log
        uses: actions/upload-artifact@v3
        with:
          name: request_log_tier1
          path: API-s-/Rate-Limit/request_log_tier1.txt

  tier2:
    name: Run AGDE Tier 2
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: API-s-/Rate-Limit
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Give permission to script
        run: chmod +x run-agde-tier2.sh

      - name: Run Tier 2 script
        run: ./run-agde-tier2.sh

      - name: Upload Tier 2 log
        uses: actions/upload-artifact@v3
        with:
          name: request_log_tier2
          path: API-s-/Rate-Limit/request_log_tier2.txt

  tier3:
    name: Run AGDE Tier 3
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: API-s-/Rate-Limit
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Give permission to script
        run: chmod +x run-agde-tier3.sh

      - name: Run Tier 3 script
        run: ./run-agde-tier3.sh

      - name: Upload Tier 3 log
        uses: actions/upload-artifact@v3
        with:
          name: request_log_tier3
          path: API-s-/Rate-Limit/request_log_tier3.txt
t
